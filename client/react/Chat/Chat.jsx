import Icon from '../Base/Icon/Icon.jsx';

var Chat = createReactClass({
	render: function () {
	    var iconSize = 20;
		return (
		    <div className='chat'>
		        Chat (128)
		    </div>
		);
	}
});

export default Chat;
